# Discord Bot Configuration
# Copy this file to .env and fill in your actual values

# Discord Bot Token
# Get this from https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Discord Channel ID
# Right-click a channel in Discord (with Developer Mode enabled) and click "Copy Channel ID"
DISCORD_CHANNEL_ID=123456789012345678

# Dropbox Access Token
# Get this from https://www.dropbox.com/developers/apps
# Create an app with files.metadata.read and files.content.read permissions
DROPBOX_ACCESS_TOKEN=your_dropbox_access_token_here

# Dropbox Folder Path (optional)
# The folder to monitor for new images
# Defaults to /Photos/gallery-dl if not specified
DROPBOX_FOLDER=/Photos/gallery-dl

# Data Directory (optional)
# Directory where the SQLite database will be stored
# Defaults to /data if not specified
DATA_DIR=/data

# Poll Interval (optional)
# How often to check Dropbox for new files (e.g., "30s", "5m", "1h")
# Defaults to 5m if not specified
POLL_INTERVAL=5m

# HTTP server port (optional)
# Defaults to 8080 if not specified
PORT=8080
